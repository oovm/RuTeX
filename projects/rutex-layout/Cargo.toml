[package]
name = "rutex-layout"
version = "0.1.0"
edition = "2021"

[dependencies]
rutex-types = { path = "../rutex-types" }
rutex-font = { path = "../rutex-font" }
serde = { version = "1.0", features = ["derive"] }
async-trait = "0.1"
