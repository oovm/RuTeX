[package]
name = "rutex-renderer-mathml"
version = "0.1.0"
edition = "2021"

[dependencies]
rutex-types = { path = "../rutex-types" }
rutex-layout = { path = "../rutex-layout" }
serde = { version = "1.0", features = ["derive"] }
