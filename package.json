{
    "private": true,
    "scripts": {
        "web": "cd examples/playground && trunk serve",
        "web:build": "cd examples/playground && trunk build --features build",
        "u": "cargo upgrade --offline --incompatible"
    }
}
